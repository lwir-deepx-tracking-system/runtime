services:
  runtime-pc:
    image: lwir-runtime-pc:dxas-5749ab70-ubuntu22.04

    volumes:
      - .:/workspace/lwir-deepx-tracking-system
      - runtime-build:/workspace/lwir-deepx-tracking-system/build

    working_dir: /workspace/lwir-deepx-tracking-system

    entrypoint: /bin/bash

    stdin_open: true
    tty: true

volumes:
  runtime-build:

# 팀 논의 사항

목표: **YOLO + ByteTrack 기준선**을 같은 조건에서 반복 실행하고, 이후 모델·처리 방식의 성능을 비교할 수 있는 기반을 합의한다.

## 내일 결정할 것

1. **Queue가 밀릴 때의 처리 방식**  
   각 stage의 queue 크기 제한과, 가득 찼을 때 대기할지 오래된 프레임을 버릴지 정한다.  
   **이유:** 처리량이 높아도 오래된 프레임이 쌓이면 화면과 짐벌의 반응이 늦어진다. 실험마다 정책이 다르면 지연·FPS 비교도 어려워진다.

2. **프레임 식별자와 시각 정보의 전달 범위**  
   카메라에서 부여한 frame ID와 획득 시각을 어느 결과까지 함께 전달할지 정한다.  
   **이유:** 검출·추적 결과를 원본 프레임과 연결하고, stage별·전체 지연을 측정하려면 필요하다.

3. **기준선의 완료 조건과 측정 항목**  
   같은 LWIR 입력으로 검출·Track ID 결과를 재현할 수 있는 상태를 기준선으로 삼을지, 어떤 정확도·지연·FPS·queue 지표를 기록할지 정한다.  
   **이유:** 이후 파인튜닝·경량화·DINO 실험의 효과를 같은 기준으로 비교하기 위해서다.

4. **실험 조건 기록 방식**  
   실행마다 사용한 YAML, 모델 파일 버전, 입력 데이터, 측정 결과를 함께 남기는 방식을 정한다.  
   **이유:** 성능 차이가 어떤 조건 변경에서 발생했는지 팀원이 다시 확인할 수 있어야 한다.

## 현재 구조에서 유지할 경계

`Camera → Preprocess → Inference → Postprocess → Tracking → 대상 선택 → STM32 통신`

Tracking은 모든 객체의 Track을 만들고, 대상 선택 단계는 GUI에서 지정한 ID를 찾는다. 실제 알고리즘, STM32 프로토콜, ID가 사라졌을 때의 제어 정책은 이번 구조 합의와 분리해 결정한다.
